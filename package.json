{
  "name": "athena",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "dev": "vite",         
    "build": "vite build", 
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist", 
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "repository": { "type": "git", "url": "git+https://github.com/Astrazo/athena.git" },
  "license": "ISC",

  "dependencies": {
    "firebase": "^11.9.1"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "gh-pages": "^6.1.1"
  }
}
